// widgets/sample_message.dart

import 'package:flutter/material.dart';

class SampleMessage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello, World"),
    );
  }
}
